<%= form_for(@deck) do |f| %>
  <% if @deck.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deck.errors.count, "error") %> prohibited this deck from being saved:</h2>

      <ul>
      <% @deck.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  
  <h2>cards</h2>
  <div class="items">
    <%= f.nested_fields_for :cards do |f|
      render 'card', f: f
    end -%>
  </div>
  <a href="#" class="add">add card</a>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
